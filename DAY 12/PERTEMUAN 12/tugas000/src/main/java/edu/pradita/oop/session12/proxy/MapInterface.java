package edu.pradita.oop.session12.proxy;

/**
 * PROXY PATTERN — Subject interface.
 *
 * Both the real map and the proxy must implement the same interface,
 * so the client can use them interchangeably.
 */
public interface MapInterface {

    // TODO P1.1
    // Declare display().
    void display();
}